// Create two variables, a and b, and assign them values. Swap the values of a and b without using a third variable.

var a = 3;
var b = 4;

// Swap the values of a and b
var temp = a;
a = b;
b = temp;

console.log("a =", a, "b =", b);
